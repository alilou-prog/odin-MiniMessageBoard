<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <ul>
        <% for (let i=0; i < messages.length; i++) { %>
            <li>
                <%=messages[i].text%>
                <a href=<%=`/details/${messages[i].id}`%> >Details</a>
            </li>
        <% } %>
    </ul>
    <a href="/new">Add a new message</a>
</body>

</html>